# AWS SDK for Python
boto3>=1.26.0

# JSON handling (built-in, but explicit for clarity)
# json - built-in module

# Logging (built-in, but explicit for clarity)  
# logging - built-in module

# Optional: Add any additional dependencies here
# requests>=2.28.0  # For HTTP requests if needed
# numpy>=1.21.0     # For numerical computations if needed